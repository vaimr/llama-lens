# llama灵境 架构设计文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-08-28 |
| 状态 | 设计阶段 |
| 上游文档 | 01-需求文档.md（需求基线） |

## 1. 总体架构

### 1.1 架构图

```
                    config/hosts.yaml (N 台主机, gitignore)
                              │
┌─────────────────────────────▼──────────────────────────────┐
│  FastAPI 单进程 :8000 (本机部署)                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ MonitorRegistry                                       │  │
│  │  ├─ HostMonitor[ai]     ├─ HostMonitor[host2] ...    │  │
│  │  │   ├─ LlamaPoller 1s  │   (每主机独立:              │  │
│  │  │   ├─ SshPoller   2s  │    采集+缓冲+事件)          │  │
│  │  │   ├─ LogPoller (journal 流)                       │  │
│  │  │   ├─ RingBuffer (3600@1s + 1800@2s)               │  │
│  │  │   └─ EventDetector (任务开始/结束/上下线)           │  │
│  └──────────────────────────────────────────────────────┘  │
│  REST: /api/hosts · /api/hosts/{id}/overview|history|events │
│  WS:   /ws/hosts/{id} (1s tick 推送, 可配置)                 │
│  静态:  frontend/dist (Vue3 构建产物)                        │
└─────────────────────────────▲──────────────────────────────┘
                              │ WS 1s / HTTP 降级
              ┌───────────────┴───────────────┐
              │  浏览器 (Vue3 + ECharts)       │
              │  /        主机门户 (卡片墙)     │
              │  /host/:id  单主机详情仪表盘    │
              └───────────────────────────────┘

数据源(每台主机):  llama HTTP API (8080) + SSH 只读批量命令 (22)
```

### 1.2 分层职责

| 层 | 组件 | 职责 |
|---|---|---|
| 配置层 | hosts.yaml + .env | 主机注册：地址/凭证/采集参数/阈值 |
| 采集层 | LlamaPoller / SshPoller / LogPoller | 每主机周期采集 + 日志流，全部只读 |
| 计算层 | SpeedCalculator / DiffEngine | 速度、CPU、磁盘、网络差分计算 |
| 存储层 | RingBuffer | 内存环形缓冲（llama 3600@1s，host 1800@2s） |
| 事件层 | EventDetector | 状态迁移检测 → 事件流 |
| 服务层 | FastAPI | REST + WS + 静态托管 |
| 展示层 | Vue3 SPA | 门户页 + 详情页 |

### 1.3 数据流

```
llama-server :8080 ──HTTP(1s)──▶ LlamaPoller ─┐
                                              ├─▶ SpeedCalculator ─▶ RingBuffer ─▶ REST/WS ─▶ 浏览器
host :22 ──SSH(2s 批量)──▶ SshPoller ─┘         │
                                                └─▶ EventDetector ─▶ EventRing ─▶ /events, WS
```

## 2. 技术选型

| 项 | 选择 | 版本 | 理由 |
|---|---|---|---|
| 后端框架 | FastAPI + uvicorn | 0.115.6 / 0.34.0 | 项目规则技术栈；异步支持；WS 内置；本机已预装 |
| SSH 客户端 | paramiko | 2.7.2 | 本机已预装；持久连接 + keepalive；无需安装 sshpass |
| HTTP 客户端 | httpx | 最新 | 异步请求 llama API（aiohttp 亦已预装，二选一） |
| 前端框架 | Vue 3 + Vite | 3.x / 5.x | 项目规则技术栈 |
| 图表 | ECharts | 5.x | 仪表盘/多序列/堆叠/暗色主题；性能良好 |
| 路由 | Vue Router | 4.x | 两级页面 |
| Python | 3.9 兼容 | 3.9.2 | 本机实际 Python 版本（非 3.11），代码避免 3.10+ 语法 |
| Node | Node 24 | v24.11.1 | 本机实际版本，Vite 兼容 |
| 部署 | 单进程直接运行（run.sh） | — | FastAPI 托管前端 dist；最简单 |

## 3. 多主机设计

### 3.1 核心抽象

```python
class HostMonitor:
    """每台主机一个独立监控单元"""
    def __init__(self, cfg: HostConfig): ...
    def start(self): ...      # 启动 LlamaPoller + SshPoller + 推送任务
    def stop(self): ...
    def snapshot(self) -> dict: ...   # 当前快照（含 alerts）
    def history(self, window_s: int) -> dict: ...
    def events(self, limit: int) -> list: ...

class MonitorRegistry:
    """管理所有 HostMonitor"""
    def load(self, path: str): ...    # 解析 hosts.yaml
    def get(self, host_id: str) -> HostMonitor: ...
    def list(self) -> list: ...       # 门户页数据（各主机摘要）
```

- 每个 HostMonitor 内部运行 3 个独立 async 任务：LlamaPoller、SshPoller、WsPusher
- 任一任务异常被捕获并记录日志，不影响其他任务与其他主机
- 启动时 Registry 加载全部主机；配置变更需重启（本期不做热加载）

### 3.2 hosts.yaml 配置（完整示例）

```yaml
# config/hosts.yaml (gitignore; 模板见 hosts.example.yaml)
global:
  push_interval: 1.0        # WS 推送间隔（秒）
  history:
    llama_points: 3600      # llama 指标环形缓冲点数 @1s（1h）
    host_points: 1800       # host 指标环形缓冲点数 @2s（1h）
  thresholds:               # 全局默认阈值
    gpu_util:   { warn: 80, danger: 90 }
    gpu_mem:    { warn: 85, danger: 95 }
    gpu_temp:   { warn: 75, danger: 85 }
    gpu_power:  { warn: 85, danger: 95 }   # 占功耗上限百分比
    cpu:        { warn: 80, danger: 90 }
    mem:        { warn: 85, danger: 95 }
    disk:       { warn: 80, danger: 90 }

hosts:
  - id: ai
    name: "AI 主机 (ai.lan)"
    llama:
      host: ai.lan
      port: 8080
      interval: 1.0         # /health + /slots 轮询间隔（秒）
      slow_interval: 30.0   # /props + /v1/models 轮询间隔（秒）
    ssh:
      host: ai.lan
      port: 22
      user: root
      password: ${AI_SSH_PASS}   # 从环境变量 .env 读取，不写明文
      interval: 2.0         # 批量采集间隔（秒）
      keepalive: 15         # SSH keepalive（秒）
    process:
      name: llama-server    # 进程监控目标进程名
    log:
      unit: llama-server    # systemd unit 名（journalctl -u）
      follow: true          # 流式跟随（false = 周期 --since 拉取）
      catchup_sec: 30       # 重连后补拉秒数
    systemd_unit: llama-server.service
    thresholds: {}          # 每主机覆盖（可选，结构同 global）
```

### 3.3 新增主机流程

1. hosts.yaml 增加一个条目（id 唯一）
2. .env 增加对应 SSH 密码（若使用环境变量引用）
3. 重启面板 → 门户页自动出现新卡片

无需改代码。

## 4. 采集层设计

### 4.1 LlamaPoller（1s）

端点与频率：

| 端点 | 频率 | 用途 |
|---|---|---|
| GET /health | 1s | 在线检测 |
| GET /slots | 1s | Slot 状态 + 速度差分输入 |
| GET /props | 30s | 模型信息、默认参数、模态 |
| GET /v1/models | 30s | 参数量/嵌入维度/词表/文件体积 |

速度差分算法（每 slot）：

```
state: prev_task_id, prev_decoded, prev_prompt_processed, prev_ts, task_start_ts

on_poll(slot):
  now = time(); dt = now - prev_ts
  if slot.is_processing:
    if prev_task_id is None:
        task_start_ts = now                      # 任务开始事件
    if slot.id_task != prev_task_id or slot.n_decoded < prev_decoded:
        # 新任务（或任务边界）：重置基线，本周期速度 = 0
        prev_task_id = slot.id_task
        prev_decoded = slot.n_decoded
        prev_prompt_processed = slot.n_prompt_tokens_processed
        gen_speed = 0; prompt_speed = 0
    else:
        gen_speed    = (slot.n_decoded - prev_decoded) / dt
        prompt_speed = (slot.n_prompt_tokens_processed - prev_prompt_processed) / dt
        prev_decoded = slot.n_decoded
        prev_prompt_processed = slot.n_prompt_tokens_processed
  else:
    if prev_task_id is not None:
        # 任务结束事件：总 token = 最后 n_decoded，耗时 = now - task_start_ts
        emit(task_end, total_tokens, duration, avg_tps)
        prev_task_id = None
    gen_speed = 0; prompt_speed = 0
  prev_ts = now
```

- 任务开始事件：is_processing false→true → 记录 task_start（任务 ID、prompt token 数）
- 模型变更：/props.model_path 变化 → model_change 事件
- 失败处理：请求超时 3s；连续 3 次失败 → online=false + llama_down 事件；恢复 → llama_up 事件；离线期间保留最后数据

### 4.2 SshPoller（2s）

连接管理：
- paramiko SSHClient，AutoAddPolicy（首次连接自动信任指纹并记录日志）
- transport.set_keepalive(15)
- 断线：指数退避重连 1s→2s→4s→...→30s 封顶；断线时 ssh_down 事件，重连成功 ssh_up 事件
- 静态信息（首次连接采集一次）：hostname、内核、OS 版本（/etc/os-release）、CPU 型号、核数

每周期批量命令（1 次 exec，一次网络往返，全部只读）：

```bash
echo ==GPU==
nvidia-smi --query-gpu=index,name,driver_version,memory.total,memory.used,memory.free,utilization.gpu,utilization.memory,temperature.gpu,power.draw,power.limit,fan.speed,clocks.current.graphics,clocks.current.memory,pcie.link.gen.current,pcie.link.width.current,pstate,temperature.memory,ecc.errors.corrected.volatile.total,ecc.errors.uncorrected.volatile.total,clocks_throttle_reasons.active,cuda_version --format=csv,noheader,nounits
echo ==APPS==
nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader,nounits
echo ==STAT==
cat /proc/stat
echo ==MEM==
grep -E '^(MemTotal|MemFree|MemAvailable|Buffers|^Cached|SwapTotal|SwapFree)' /proc/meminfo
echo ==LOAD==
cat /proc/loadavg
echo ==UPTIME==
cat /proc/uptime
echo ==NET==
cat /proc/net/dev
echo ==DISKIO==
grep -E '^(sd|nvme|vd|dm)' /proc/diskstats
echo ==DF==
df -B1 --output=target,size,used,avail,pcent / /share 2>/dev/null
echo ==PROC==
PID=$(pgrep -x llama-server | head -1)
if [ -n "$PID" ]; then
  echo P:$PID
  awk '{print $14, $15, $23}' /proc/$PID/stat    # utime stime vsize
  grep -E '^(VmRSS|VmSize|Threads)' /proc/$PID/status
  ps -o pcpu=,pmem=,etime= -p $PID
  tr '\0' ' ' < /proc/$PID/cmdline; echo
fi
echo ==PS==
ps -eo pid,comm,pcpu,pmem,rss --no-headers 2>/dev/null
echo ==PSTICKS==
awk 'FNR==1 { n=split(FILENAME, p, "/"); pid=p[n-1]; i=index($0, ") "); if (i > 0) { s=substr($0, i+2); m=split(s, a, " "); if (m >= 13) print pid, a[12], a[13] } }' /proc/[0-9]*/stat 2>/dev/null
echo ==PROCS==
ls /proc | grep -c '^[0-9]'
```

差分计算（后端 DiffEngine，基于连续两次采样）：
- 整机 CPU% = 1 - Δidle/Δtotal（cpu 汇总行）
- 每核 CPU% = 1 - Δidle_i/Δtotal_i（cpu0..cpu5 行）
- 进程实时 CPU% = Δ(utime+stime)/Δt/CLK_TCK（相对单核）
  - 进程卡：/proc/<pid>/stat 第 14/15 字段（时钟滴答，10ms 精度）
  - Top 列表：==PSTICKS== 段（单条 awk 读全部 /proc/<pid>/stat 的 utime/stime，10ms 精度，按 PID 与 ==PS== 段关联）
  - 注意：procps 的 stime 列是启动时间（start time）而非系统 CPU 时间，utime 列不可用（输出 -）；time 列仅 1s 精度，2s 采样窗口下 CPU% 会被量化成 0/50/100/150 台阶值（空闲时全 0 看似"数据是假的"），故必须直接读 /proc
- Top CPU 排序：实时 CPU% → 累计 CPU 时间 → RSS；Top MEM 排序：RSS → 实时 CPU%（各取前 8，后端排序）
- 磁盘读写速率 = Δsectors×512/Δt
- 网络 rx/tx 速率 = Δbytes/Δt
- 首次采样无基线 → 本周期差分指标为 null

### 4.3 EventDetector

状态机（每主机）：

```
llama: online ⇄ offline   （连续 3 次 /health 失败 → offline）
ssh:   connected ⇄ disconnected
task:  idle → running (task_start) → idle (task_end, 带统计)
model: props.model_path 变化 → model_change
prefill: 每任务首条 prompt 行 → prefill_start（含 prompt 总 token 数）
alert:   check_alerts(alerts) 按 metric 跟踪级别变化（normal/warn/danger），变化时发事件（两次事件最小间隔 30s，首次观测只记基线）
```

事件环形：200 条，字段 {ts, level, type, msg}
- task_start/task_end 优先取日志行（launch_slot_ / release）为源，含完整统计（MTP 接受率、上下文占用、耗时）
- boot 事件：日志 "model loaded" 行 → llama_boot（含启动信息）
- prefill_start：LogPoller 在每任务首条 prompt 行发出（按任务 ID 去重，任务结束/重启重置）
- alert：HostMonitor 每次快照 evaluate_alerts 后调用 check_alerts；llama/ssh 不重复（已有专门上下线事件）

### 4.4 LogPoller（日志流）

数据源：`journalctl -u {log.unit}`（systemd journal，只读）

采集方式：
- 专用 SSH channel（与 SshPoller 同一 transport，独立 channel）运行 `journalctl -u llama-server -f -o short-iso --no-pager -n 0` 流式跟随
- channel 断开：重连 + 先 `--since "N seconds ago"`（catchup_sec，默认 30s）补拉，防丢行
- follow=false（兜底模式）：周期（2s）`--since "@<last_ts>"` 拉取新行
- 首次连接拉取最近一次启动块（"listening" 前 200 行）解析 boot 信息

日志行格式（short-iso）：

```
2026-08-28T18:11:25+08:00 ai llama-server[55646]: 50.44.267.087 I slot      release: id  0 | task 14847 | stop processing: n_tokens = 201047, truncated = 0
└─ syslog 时间 ──────────┘ └host┘ └unit[pid]┘  └─ llama.cpp 内部时间 ──┘ └L┘ └subsys┘ └function┘ └─ 消息 ─┘
```

解析规则（正则，按序匹配）：

| # | 模式（简化） | 事件 | 提取字段 |
|---|---|---|---|
| 1 | `slot print_timing: id (\d+) \| task (\d+) \| n_decoded = (\d+), tg = ([\d.]+) t/s, tg_3s = ([\d.]+) t/s` | decode_tick | task_id, n_decoded, tg, tg_3s |
| 2 | `slot print_timing: id (\d+) \| task (\d+) \| prompt processing, n_tokens = (\d+), progress = ([\d.]+), t = ([\d.]+) s / ([\d.]+) tokens per second` | prompt_tick | task_id, n_tokens, progress, elapsed_s, speed_tps |
| 3 | `slot print_timing: ... prompt eval time = ([\d.]+) ms / (\d+) tokens \(([\d.]+) ms per token, ([\d.]+) tokens per second\)` | task_summary.prompt | prompt_ms, prompt_tokens, prompt_speed_tps |
| 4 | `slot print_timing: ... eval time = ([\d.]+) ms / (\d+) tokens \(([\d.]+) ms per token, ([\d.]+) tokens per second\)` | task_summary.eval | eval_ms, decoded_tokens, gen_speed_tps |
| 5 | `slot print_timing: ... total time = ([\d.]+) ms / (\d+) tokens` | task_summary.total | total_ms, total_tokens |
| 6 | `slot print_timing: ... graphs reused = (\d+)` | task_summary.graphs | graphs_reused |
| 7 | `slot print_timing: ... draft acceptance = ([\d.]+) \((\d+) accepted / (\d+) generated\), mean len = ([\d.]+)` | task_summary.mtp | acceptance, accepted, generated, mean_len |
| 8 | `slot release: id (\d+) \| task (\d+) \| stop processing: n_tokens = (\d+), truncated = (\d+)` | task_end | ctx_used, truncated |
| 9 | `slot get_availabl: id (\d+) \| task -1 \| selected slot by (LRU\|LCP similarity)(?:, f_sim_best = ([\d.]+).*)?(?:, f_keep = ([\d.]+))?` | slot_select | method, f_sim_best, f_keep |
| 10 | `slot launch_slot_: id (\d+) \| task (\d+) \| processing task, is_child = (\d+)` | task_start | task_id, is_child |
| 11 | 启动行：`loading model '(.+)'` / `n_slots = (\d+), n_ctx_slot = (\d+), kv_unified = '(\w+)'` / `creating MTP draft context` / `upgrading K from (\S+) to (\S+)` / `verbosity = (\d+)` / `listening on (\S+)` / `W` 级行 | boot_info | model, n_slots, n_ctx_slot, kv_unified, mtp_draft, kv_upgrade, verbosity, listening, warnings[] |

状态机（当前状态，逐行更新）：

```
phase: idle → prompt_processing (prompt_tick) → decoding (decode_tick) → idle (task_end)
current: {phase, task_id, n_decoded, tg, tg_3s, prompt_progress, prompt_speed, prompt_total_tokens, prompt_elapsed_s, is_child, started_at}
started_at: 任务开始时间戳（launch 行写入，task_end 清空；前端算"已运行"时长）
last_prefill: {speed, ts, progress, n_tokens}   # 每条 prompt 行更新，llama 重启清空
context: {used (最近 task_end 的 n_tokens), total (boot n_ctx_slot), pct, remaining, truncated}
mtp:     {acceptance, accepted, generated, mean_len}   # task_end 时更新
kv:      {f_keep, f_sim_best, selection}               # slot_select 时更新
boot:    {...}                                          # boot_info 时更新
```

速度来源优先级：
1. 日志 tg_3s（3 秒窗口，服务端计算）—— 主
2. /slots n_decoded 差分（1s）—— 日志不可用时兜底
- 前端展示标注数据来源（log / API）

失败处理：
- channel 断开 → 重连 + 补拉；期间 log_available=false，速度回退 /slots 差分
- 行解析失败 → 跳过并计数（parse_error 计数器），不影响其他行
- llama-server 重启（PID 变化）→ 重置状态机，重新解析 boot 信息

## 5. 数据模型

### 5.1 快照（GET /api/hosts/{id}/overview 响应）

```jsonc
{
  "ts": 1724000000.0,
  "host": { "id": "ai", "name": "AI 主机 (ai.lan)" },
  "llama": {
    "online": true,
    "model": {
      "name": "Qwen3.8-27B-Q6_K", "path": "/share/AI/LLM/unsloth/Qwen3.8-27B-Q6_K.gguf",
      "ftype": "Q6_K", "n_params": 27320697856, "n_embd": 5120, "n_vocab": 248320,
      "n_ctx": 262144, "n_ctx_train": 262144, "vocab_type": 2,
      "file_size": 22873411584,
      "mmproj_path": "/share/AI/LLM/unsloth/Qwen3.8-27B-mmproj-BF16.gguf",
      "mmproj_size": 889000000,
      "modalities": { "vision": true, "video": true, "audio": false },
      "capabilities": ["completion", "multimodal"], "owned_by": "llamacpp"
    },
    "gen_speed_tps": 42.3,
    "prompt_speed_tps": 123.4,
    "speed_source": "log",
    "log": {
      "available": true,
      "state": {
        "phase": "decoding",
        "task_id": 14847, "n_decoded": 2843, "is_child": 0,
        "tg_3s_tps": 28.46, "tg_tps": 26.21,
        "prompt_progress": null, "prompt_speed_tps": null,
        "prompt_total_tokens": null, "prompt_elapsed_s": null,
        "started_at": 1756361595.0
      },
      "last_prefill": { "speed": 516.98, "ts": 1756361595.0, "progress": 0.92, "n_tokens": 2048 },
      "context": { "used": 201047, "total": 262144, "pct": 76.7,
                   "remaining": 61097, "truncated": false },
      "mtp": { "acceptance": 0.698, "accepted": 1925, "generated": 2757,
               "mean_len": 3.09,
               "config": { "spec_type": "draft-mtp", "spec_draft_n_max": 3 } },
      "kv": { "f_keep": 1.0, "f_sim_best": 0.988, "selection": "LCP" },
      "graphs_reused": 15340,
      "boot": {
        "pid": 55646, "started_at": "2026-08-28T17:20:41",
        "verbosity": 3, "n_slots": 1, "n_ctx_slot": 262144, "kv_unified": false,
        "mtp_draft": true,
        "kv_cache_upgrade": "turbo3 -> q8_0 (K, auto-asymmetric GQA 6:1)",
        "listening": "http://0.0.0.0:8080",
        "warnings": ["CORS is set to allow all origins ('*') and no API key is set",
                     "NOTICE: server default port will be changed to :9931 in a future release"]
      },
      "last_task": {
        "task_id": 14847,
        "prompt_ms": "...", "prompt_tokens": "...", "prompt_speed_tps": "...",
        "eval_ms": 110069.09, "decoded_tokens": 2843, "gen_speed_tps": 25.83,
        "total_ms": 123991.42, "total_tokens": 7569,
        "graphs_reused": 15340,
        "mtp": { "acceptance": 0.698, "accepted": 1925, "generated": 2757, "mean_len": 3.09 },
        "ctx_used": 201047, "truncated": false
      }
    },
    "slots": [
      {
        "id": 0, "is_processing": true, "id_task": 5199, "n_ctx": 262144,
        "n_prompt_tokens": 19032, "n_prompt_tokens_processed": 626,
        "n_prompt_tokens_cache": 0, "n_decoded": 795, "n_remain": 31205,
        "ctx_used": 19827, "ctx_pct": 7.6,
        "gen_speed_tps": 42.3, "prompt_speed_tps": 123.4,
        "speculative": true,
        "params": { "temperature": 0.3, "top_k": 40, "top_p": 0.9, "max_tokens": 32000, "...": "..." }
      }
    ]
  },
  "host_metrics": {
    "reachable": true,
    "sys": { "hostname": "ai", "kernel": "6.8.0-124-generic", "os": "Ubuntu 24.04",
             "uptime_s": 28320, "procs": 293 },
    "cpu": { "model": "Intel Core i5-8500", "cores": 6, "usage_pct": 12.3,
             "per_core_pct": [15, 10, 12, 11, 13, 12],
             "load": [1.09, 0.75, 0.58], "mhz": 3000 },
    "mem": { "total_mb": 31744, "used_mb": 5939, "free_mb": 2765,
             "buff_cache_mb": 25600, "available_mb": 25600,
             "swap_total_mb": 8192, "swap_used_mb": 0 },
    "disk": { "mounts": [ { "mount": "/", "size_gb": 1234, "used_gb": 801,
                            "avail_gb": 347, "use_pct": 70 } ],
              "read_mb_s": 1.2, "write_mb_s": 0.3 },
    "net": { "ifaces": [ { "name": "eth0", "rx_mb_s": 0.5, "tx_mb_s": 0.2,
                           "rx_total_mb": 12345, "tx_total_mb": 678 } ] },
    "gpus": [
      { "index": 0, "name": "NVIDIA GeForce RTX 3080", "driver": "580.159.03",
        "util_pct": 0, "mem_util_pct": 0,
        "mem_total_mb": 20480, "mem_used_mb": 18459, "mem_free_mb": 2021,
        "temp_c": 54, "power_w": 141.5, "power_limit_w": 280,
        "fan_pct": null, "clock_mhz": 1440, "mem_clock_mhz": 1188,
        "pcie_gen": 3, "pcie_width": 16, "pstate": "P2",
        "temp_mem_c": null, "ecc_corrected": null, "ecc_uncorrected": null, "throttle": 0,
        "apps": [ { "pid": 31255, "name": "llama-server", "mem_mb": 18414 } ] }
    ],
    "process": {
      "found": true, "pid": 31255,
      "cpu_pct_realtime": 45.2, "cpu_pct_lifetime": 98.6, "mem_pct": 16.0,
      "rss_mb": 5153, "vsz_mb": 12000, "threads": 10, "elapsed": "02:28",
      "cmdline": "/home/wx/llama-cpp-turboquant-new/build/bin/llama-server -m ... --port 8080",
      "flags": { "model": "...", "mmproj": "...", "n_gpu_layers": "all",
                 "flash_attn": "enabled", "tensor_split": "24,20",
                 "batch": 1024, "ubatch": 512, "np": 1, "ctx_size": 262144,
                 "kv_offload": true, "cache_type_k": "turbo3", "cache_type_v": "turbo3",
                 "fit": "off", "threads": 6, "threads_batch": 6, "threads_http": 1,
                 "temperature": 0.3, "top_p": 0.9, "top_k": 40,
                 "spec_type": "draft-mtp", "spec_draft_n_max": 3,
                 "port": 8080, "host": "0.0.0.0" }
    },
    "service": {
      "unit": "llama-server.service",
      "description": "llama-server Qwen3.8-27B (TurboQuant)",
      "active": "active (running)", "since": "2026-08-28 15:53:10",
      "cpu_total": "9min 57.800s", "memory": "4.6G", "memory_peak": "4.6G", "tasks": 10
    },
    "top": {
      "cpu": [ { "pid": 31255, "name": "llama-server", "cpu_pct": 98.6,
                 "mem_pct": 16.0, "rss_mb": 5153 } ],
      "mem": [ "..." ]
    }
  },
  "alerts": [
    { "metric": "gpu0.util", "level": "danger", "value": 92.0, "threshold": 90 }
  ]
}
```

### 5.2 环形缓冲

- llama 序列（1s）：gen_speed（tg_3s）、prompt_speed、ctx_used（1s 采样，API 实时值优先 / 日志兜底；任务结束点由 LogPoller 另行写入，为权威值）
- log 序列（按事件）：context.used（n_tokens，task_end 时更新）、mtp.acceptance（task_end 时更新）
- host 序列（2s）：cpu、per_core[]、load_1/load_5/load_15、mem_used、mem_buff_cache、swap_used、每卡 util/mem/temp/power、disk rw、net rx/tx、process cpu
- 实现：collections.deque(maxlen=N) + ts 数组；history API 按窗口切片并降采样（点数 > 600 时取每 k 个）

### 5.3 事件

- deque(maxlen=200)，{ts, level, type, msg}

## 6. API 设计

### 6.1 REST

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/health | 面板自检：{status, hosts: {id: {llama_online, ssh_ok}}} |
| GET | /api/hosts | 门户页：[{id, name, online, model_name, n_params, gen_speed_tps, gpus: [{util_pct, mem_pct}], cpu_pct, mem_pct, alerts_count}] |
| GET | /api/hosts/{id}/overview | 完整快照（§5.1） |
| GET | /api/hosts/{id}/history?window=300 | 历史序列（window 秒：300/900/3600） |
| GET | /api/hosts/{id}/events?limit=50 | 事件流 |

history 响应：

```jsonc
{
  "ts": [1724000000, 1724000001, "..."],
  "gen_speed": [0, 42.1, "..."],
  "prompt_speed": [120.5, 0, "..."],
  "gpu_util": [[0, 87, "..."], [0, 92, "..."]],
  "gpu_mem": [[18459, "..."], [17639, "..."]],
  "gpu_temp": [[54, "..."], [49, "..."]],
  "cpu": [12.3, "..."],
  "mem_used": [5939, "..."],
  "mem_buff_cache": [25600, "..."],
  "net_rx": [0.5, "..."], "net_tx": [0.2, "..."],
  "proc_cpu": [45.2, "..."],
  "load_1": [1.09, "..."], "load_5": [0.75, "..."], "load_15": [0.58, "..."],
  "ctx_used": [130453, 135504, "..."],
  "mtp_acceptance": [0.931, 0.850, "..."]
}
```

### 6.2 WebSocket

- 路径：/ws/hosts/{id}
- 服务端 → 客户端：每 push_interval（默认 1s）推送快照（结构同 overview）
- 客户端 → 服务端：{"type": "ping"}（心跳，10s 一次）
- 服务端 → 客户端：{"type": "pong"}
- 心跳超时 30s → 服务端断开；客户端自动重连（1s/2s/4s 退避），断线期间降级为 HTTP 轮询
- 门户页：/ws/portal 每 1s 推送 /api/hosts 数据

## 7. 前端架构

- 路由：/（Portal）/host/:id（Detail）
- 状态：模块级 reactive store（不引入 Pinia，场景简单）+ 页面级 composable
- WS 客户端：useHostStream(hostId) composable —— WS 为主，3 次心跳失败自动降级轮询，暴露 {snapshot, connected, mode}
- 组件树：

```
App
├─ PortalView
│  ├─ BrandBar
│  └─ HostCard × N
└─ HostDetailView
   ├─ TopBar
   ├─ OverviewSection (BarCard × 3 + GaugeCard × 1)
   ├─ TaskSection (LlamaStateCard + EventFeed)
   ├─ GpuSection (GpuPanel × N)
   ├─ SystemSection (CpuPanel, MemPanel, DiskPanel, NetPanel)
   ├─ ProcessSection (LlamaProcessCard, TopProcessTable × 2)
   ├─ ModelSection (ModelInfoCard, SlotTable)
   └─ TrendSection (TrendChart × 12，3 组：llama/GPU/系统)
```

- ECharts：共享暗色主题配置；图表用 setOption 更新（不重建实例）；动画关闭（animation: false，监控图即时刷新不闪烁）
- 数字：count-up 动画（requestAnimationFrame，300ms）

## 8. 阈值引擎

- 评估时机：每次快照生成时（后端），产出 alerts[]（metric/level/value/threshold）
- 前端只渲染：level=danger → 红色样式，warn → 黄色样式
- 配置：全局默认 + 每主机覆盖（见 hosts.yaml）

## 9. 部署与运维

- 目录结构（实现阶段）：

```
/data/case/LlamaLens/
├── backend/
│   ├── main.py            # FastAPI 入口、静态托管、Registry 启动
│   ├── config.py          # hosts.yaml + .env 解析
│   ├── store.py           # RingBuffer
│   ├── diff.py            # DiffEngine
│   ├── events.py          # EventDetector
│   ├── api.py             # REST 路由
│   ├── ws.py              # WS 路由
│   ├── pollers/
│   │   ├── llama_api.py
│   │   └── ssh_host.py
│   └── requirements.txt
├── frontend/
│   ├── package.json
│   ├── vite.config.js     # dev proxy /api,/ws → :8000
│   ├── index.html
│   └── src/
│       ├── main.js, App.vue, router.js
│       ├── api.js, stream.js
│       ├── theme/echarts-dark.js
│       ├── views/PortalView.vue, HostDetailView.vue
│       └── components/ (15 个组件, 见 03-UI与交互设计文档.md §4)
├── config/
│   ├── hosts.yaml         # gitignore
│   └── hosts.example.yaml
├── .env                   # gitignore (AI_SSH_PASS=...)
├── .env.example
├── .gitignore
├── Dockerfile             # 多阶段构建：node 构建前端 → python slim 运行后端
├── .dockerignore
├── docker-compose.yml     # docker compose up -d --build
├── run.sh                 # uvicorn backend.main:app --host 0.0.0.0 --port 8000
└── README.md
```

- 启动：./run.sh（首次：cd frontend && npm install && npm run build）
- Docker 部署：多阶段 Dockerfile（node:20-alpine 构建前端 → python:3.11-slim 运行后端，
  代码兼容 Python 3.9+），`docker compose up -d --build` 或
  `docker build -t llamalens:latest . && docker run ...`；
  config/hosts.yaml 与 .env 运行时只读挂载（不打进镜像），logs/ 挂载到宿主机；
  内置 HEALTHCHECK（/api/health）；端口由 PORT 环境变量控制（默认 8000）
- 日志：uvicorn stdout + 后端日志（logging，INFO，输出到 logs/llamalens.log）
- 资源开销预估：Python 进程 ~150-300MB（含历史缓冲），CPU < 5% 单核；每主机每 2s 一条 SSH 命令
- 端口：8000（env PORT 可配置）

## 10. 安全设计

- 凭证：.env（gitignore）+ hosts.yaml 引用环境变量；hosts.yaml 本身也 gitignore（含主机拓扑）；仅提交 hosts.example.yaml 模板
- SSH：密码认证（现状），预留 key 认证（config 字段 key_path）
- 采集命令全部只读，无任何写操作
- 面板监听 0.0.0.0:8000，局域网内使用，无认证（风险已声明，见 NFR-06）
- 前端代码不含凭证；API 不返回密码字段

## 11. 扩展性设计

| 扩展点 | 方式 |
|---|---|
| 新增主机 | hosts.yaml 加条目 |
| 新增指标 | SshPoller 批量命令 +1 行 + 快照字段 + 前端面板 |
| 新增事件类型 | EventDetector 状态机 +1 迁移 |
| --metrics 接入 | 若实例启用 --metrics，LlamaPoller 优先取 /metrics（llama_tokens_generated_total 等），差分法兜底 |
| 同主机多进程 | process.name 支持列表，snapshot.processes[] |
| 历史持久化 | RingBuffer 增加 writer（SQLite），API 不变 |

## 12. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 本机 Python 3.9.2 | 代码 3.9 兼容（不用 match/3.10+ 语法）；依赖全部已预装 |
| /metrics 未启用 | 差分法为主（实测 /slots 字段足够） |
| SSH 断连 | keepalive + 指数退避重连 + 降级显示 |
| llama-server 重启（换模型） | id_task 重置基线；/props 30s 刷新；model_change 事件 |
| 1s 轮询负载 | /slots 为轻量只读 JSON（~2KB）；--threads-http 1 影响可忽略；间隔可配置 |
| IPv6 主机 | paramiko/requests 原生支持 IPv6（已实测连通） |
| GPU1 PCIe x4（异常链路） | 如实展示，用户可在页面判断 |
| 日志格式变化（llama.cpp 版本升级） | 解析规则集中一处（正则表），单点修改；解析失败只跳过该行不影响整体；速度有 /slots 差分兜底 |
| journal 日志量（print_timing 每 3s 一行） | 仅流式跟随 + 首次拉启动块 200 行，不读全量历史；单行解析 O(1) |

## 13. 本地 CLI（llamalens）

在**被监控主机本地**运行的 htop 风格全屏 TUI，与 Web 面板单主机详情页同源（同一套采集字段、阈值、事件、日志解析规则）。零运行时依赖：单个静态二进制（`CGO_ENABLED=0`），目标主机无需 Go / Python / pip。

### 13.1 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Go 1.24 | 单静态二进制、跨平台、`/proc` 直读方便 |
| TUI 框架 | bubbletea + lipgloss（charm） | 托管终端生命周期（raw 模式 / 备用屏 / 光标 / resize / 退出恢复），**不手写任何 ANSI 转义**——规避早期手写转义导致的全屏乱码 |
| 渲染 | lipgloss 样式 + 自绘 sparkline/条 | 与前端 Terminal 主题同色（cyan/green/amber/red） |

### 13.2 架构（单进程，无网络依赖）

```
llamalens（单进程）
├── collect.LlamaCollector   本机 HTTP：/slots@1s、/props+/v1/models@30s
├── collect.HostCollector    /proc 直读@2s（stat/meminfo/loadavg/net/dev/diskstats/[pid]/stat）+ nvidia-smi@2s + systemctl show
├── logparse.Poller          11 类日志行正则 + 状态机（与 backend/pollers/log_poller.py 同源）
├── collect.JournalCollector journalctl -u <unit> -f 流式（断线 --since 补拉；PID 变化重拉 boot 块）
├── model.World              聚合：差分引擎 + 环形缓冲（llama@1s/3600、host@2s/1800）+ 事件检测 + 告警评估
└── ui.App（bubbletea）      4fps 渲染；q/p/t/g 按键；WindowSizeMsg 自适应
```

数据流与 Web 版一致：采集器 → World.Tick（1s：速度优先级 log tg_3s > /slots 差分、写环、构建快照、评估告警）→ 快照 → 渲染。

任务卡状态以 **/slots API 为准**（任一 slot `is_processing` 即生成中）：journal 流有延迟/丢行，任务开始时日志 phase 滞后于 API，若以日志 phase 为准会出现「Token 速度有值、状态却空闲」。日志 phase 仅用于细化标签（解码中/预填充）；生成中且日志未跟上时显示「生成中」。任务 ID 同理：生成中以 `slot.id_task` 为准（日志 task_id 可能是上一任务残留）。

nvidia-smi 采集细节：`--query-gpu` 含 `uuid`、`--query-compute-apps` 含 `gpu_uuid`，进程**按卡归属**（每张卡「占用」行显示该卡显存，同一 PID 在用到的每张卡各出现一行；旧驱动无 `gpu_uuid` 时回退全部挂第一张卡）。注意 CSV 逗号后带空格（`" 17920"`），数值解析必须 trim，否则 `ParseInt` 失败恒为 0（曾致「GPU 占用 0MB」）。

### 13.3 渲染正确性约束（关键）

早期版本手写 ANSI 转义导致全屏乱码（报文状）。重写后强制三条约束，PTY 实测验证：

1. **不手写转义**：终端生命周期全交 bubbletea；样式全走 lipgloss。
2. **ANSI 感知宽度/截断**：`displayWidth`/`trunc` 按字节跳过转义序列（CSI 引导符 `[` 与终止字节区分），CJK 计 2 列；绝不在转义序列中间截断（否则颜色码泄漏 + 行宽溢出）。
3. **行宽 ≤ 终端宽-1**：写满最后一列会触发终端自动换行，导致后续行整体下移、跨帧重叠。`Render()` 末尾对每行 `trunc(l, w-1)` 兜底；lipgloss `Width/Height` 为内尺寸（不含边框），面板外尺寸需先减 2。
4. **TopBar 截断优先级**：左侧分段为「品牌 · 主机 · 模型  在线状态」，空间不足时先截模型名、再截主机名，保证在线状态始终可见（整段 left 截断会先砍掉末尾的在线状态）。
5. **边框色比前端亮**：前端卡片边框 `#242428` 依赖卡片底色，TUI 无底色时在深色终端几乎不可见，故 TUI 边框用 `#55555e`（其余颜色与前端 Terminal 主题一致）。
6. **各行右缘对齐**：每行总宽必须恰好等于可用宽（终端宽−2）。`lipgloss.JoinHorizontal` 不插入分隔列（易误以为占位——旧代码预留 (n−1) 个 gap 列但实际不存在，导致各行右缘参差）。分栏时末列取余数：`cw=usable/n`、`cwLast=usable−cw×(n−1)`。

### 13.4 配置（flag，默认与 ai.lan 一致）

`--llama-host 127.0.0.1 --llama-port 8080 --process llama-server --unit llama-server --log journal --mounts / --once --dump-frame <file> --no-color`。`--dump-frame` 为排查用：每次渲染帧的原始字节（含 ANSI）写入文件（每次覆盖），复现显示问题时按 `p` 暂停即保留该帧。`--no-color` 为排查用：`lipgloss.SetColorProfile(termenv.Ascii)` 纯文本渲染（lipgloss 渲染时读全局 profile，启动前设置即可全局生效）。进程名 / unit / 端口须与 `hosts.yaml` 对应主机一致（见 README「准备被监控主机」四条件）。`--process` 进程名先 `/proc/<pid>/comm` 精确匹配，失败再回退 cmdline argv[0] basename 匹配（内核把 comm 截断到 15 字符，定制构建可直接传全名，如 `llama-cpp-turboquant`）。TopBar 模型名与 Web TopBar 一致：alias 为完整路径时只显示文件名；模型卡显示完整名 + 体积 + mmproj（mmproj 来自进程命令行 `--mmproj`）。

### 13.5 与 Web 面板的关系

CLI 是**只读旁路**：不经过后端、不写任何文件、不改变 llama-server 状态。采集字段、阈值、事件类型、日志解析正则与后端同源（后端为 Python、CLI 为 Go，规则各自维护但语义一致）。后端升级采集字段时，需同步检查 CLI 是否需要对齐。
