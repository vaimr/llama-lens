# llama灵境 需求文档

| 项 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | 2026-08-28 |
| 状态 | 设计阶段（后续开发基线） |
| 项目路径 | /data/case/LlamaLens |
| 关联文档 | 02-架构设计文档.md、03-UI与交互设计文档.md |

> 本文档是后续开发的主依据。任何需求变更必须先更新本文档，再改代码。

## 1. 背景与目标

### 1.1 背景

- 局域网内存在 llama.cpp llama-server 推理实例（首台：ai.lan），运行 Qwen3.8-27B-Q6_K（27.32B 参数，双 RTX 3080 tensor split 推理，带视觉能力）。
- 目前没有任何可视化手段观察实例负载：token 生成速度、GPU 利用率/显存/温度/功耗、CPU、内存、进程状态等只能靠 SSH + nvidia-smi + 手工命令查看。
- 后续会接入更多推理主机，需要"每台主机一个页面 + 统一门户"的监控方案。

### 1.2 目标

1. 构建 Web 监控面板 llama灵境，部署在本机（单进程，端口 8000），远程监控 N 台 llama-server 主机。
2. 每台主机有独立详情仪表盘，门户页聚合所有主机。
3. 数据 1 秒粒度实时刷新（可配置），高科技、动态的视觉风格。
4. 详细展示所有 AI/llama 相关信息（80+ 数据项，完整清单见 §7）。
5. 纯展示、不做告警通知，但指标超阈值必须视觉飘红。

### 1.3 非目标（本期）

- 不做告警推送（邮件/IM/webhook）
- 不做历史数据持久化（仅内存环形缓冲，重启丢失）
- 不做用户认证（局域网内部工具）
- 不修改被监控主机任何配置（不加 --metrics 参数、不装 agent）
- 不做 Docker/K8s 打包（run.sh 直接运行；Docker 为后续迭代）

## 2. 术语

| 术语 | 说明 |
|---|---|
| llama-server | llama.cpp 官方 HTTP 推理服务（本实例为定制构建 llama-cpp-turboquant） |
| Slot | llama-server 并发会话槽位（-np 1 表示 1 个 slot） |
| tok/s | 每秒生成 token 数 |
| Prompt 处理速度 | prompt token 预填充速度（tok/s） |
| Tensor split | 模型层切分到多 GPU（-ts 24,20） |
| 投机解码 | draft-mtp 投机解码（--spec-type draft-mtp --spec-draft-n-max 3） |
| 主机 | 一台被监控的推理机（在 hosts.yaml 中注册） |
| 门户页 | 列出所有主机的一级页面（/） |
| 详情页 | 单主机仪表盘（/host/:id） |
| 阈值飘红 | 指标超过阈值时的视觉高亮（黄=警告 / 红=危险） |

## 3. 用户与使用场景

- **用户**：AI 基础设施运维 / 开发者（局域网内）
- **使用场景**：
  1. 日常巡检：打开门户页，一眼看到所有主机状态
  2. 任务监控：长推理任务运行时，实时观察 token 速度与 GPU 负载
  3. 故障排查：任务变慢时，查看 GPU 利用率/温度/功耗/PCIe 链路是否异常
  4. 容量评估：观察内存/磁盘/上下文占用，判断实例是否接近上限

## 4. 功能需求

### FR-01 多主机管理与配置

- 主机在 `config/hosts.yaml` 注册：id、名称、llama 地址（host:port）、SSH（host/port/user/password）、采集间隔、阈值覆盖
- 增删主机只需改配置 + 重启服务，不改代码
- 每台主机独立监控：一台故障不影响其他主机与面板自身
- 首台主机为 ai.lan（实测环境见附录 A）

### FR-02 主机门户页（/）

- 顶部品牌栏：llama灵境 标识、主机总数 / 在线数
- 主机卡片墙（网格布局），每卡展示：
  - 主机名 + 状态点（在线绿脉冲 / 离线红 / SSH 断开黄）
  - 模型名 + 参数量
  - Token 生成速度（大数字 + 60s sparkline）
  - 每 GPU 一条利用率条（每卡一行）
  - CPU 利用率、内存使用
  - 超阈值飘红（红边框 + 红色角标）
- 点击卡片进入详情页 /host/:id
- 门户页同样实时刷新（1s）

### FR-03 单主机详情仪表盘（/host/:id）

- 完整布局见 03-UI与交互设计文档.md §3；分区为：
  TopBar / 实时总览区（4 卡：3 条卡 + 1 仪表）/ GPU 区（按卡聚合）/ 实时生成任务区（状态卡 + 事件流）/ 系统区 / 进程区 / 模型与 Slot 区 / 趋势区（12 图 3 组）

### FR-04 AI 核心指标

- 展示位置：速度 / 上下文占用为条卡（上下文占用大数字为原始 token 数，百分比在右上角）、MTP 接受率为仪表（值驱动颜色），均位于实时总览区（见 03-UI与交互设计文档 §3.3）；任务状态与事件流位于实时生成任务区（§3.4）
- Token 生成速度（tok/s）：实时值 + sparkline + 历史曲线
  - 主数据源：日志 tg_3s（3 秒窗口实时速度，服务端计算，更准）；兜底：/slots n_decoded 差分
- Prompt 处理速度（tok/s）：同上
  - 主数据源：日志 prompt processing 行（实时速度 + 进度 %）；兜底：/slots n_prompt_tokens_processed 差分
- 上下文占用：已用/总量及百分比
  - 主数据源：日志 release 行 n_tokens（权威值，含系统提示词）；兜底：/slots（n_prompt_tokens + n_decoded）
  - 展示：已用（原始 token 数大数字）、百分比（右上角）、剩余、总量（n_ctx，条满量程与刻度）、是否被截断（truncated）
- 任务状态：运行中/空闲、任务 ID、剩余 token（n_remain）、当前任务累计已解码（n_decoded）
- 任务级统计：任务结束时记录总 token 数、耗时、平均速度、MTP 接受率、上下文占用（进入事件流）

### FR-05 GPU 监控（按卡聚合）

- 每张 GPU 一个独立面板，包含该卡全部指标：
  - 利用率（环形仪表）
  - 显存：used/free/total + 进度条
  - 温度、功耗（draw/limit）、风扇转速、图形频率、显存频率
  - PCIe 链路（gen/width）、性能状态（P-state）
  - 驱动版本、卡型号
  - 占用该卡的进程（pid/name/显存）
- 历史曲线：利用率、显存（每卡一条线）

### FR-06 系统资源监控

- CPU：型号、核数、整机利用率、每核利用率（每核一根条）、load 1/5/15、主频
- 内存：total/used/free/buff_cache/available、swap used/total
- 磁盘：每挂载点 size/used/avail/百分比 + 读写速率
- 网络：每网卡 rx/tx 速率与累计
- 系统：主机名、内核、OS 版本、uptime、进程总数

### FR-07 进程监控

- llama-server 进程卡：
  - PID、实时 CPU%（差分）、累计 CPU%、RSS、VSZ、线程数、运行时长
  - systemd 服务状态（unit 名/描述/active/启动时间/服务级 CPU 累计/服务级内存含峰值/Tasks 数）
  - 完整命令行（可折叠）+ 解析参数表（model、mmproj、n-gpu-layers、flash-attn、tensor-split、batch、ubatch、np、ctx-size、kv-offload、cache-type-k/v、fit、threads、threads-batch、threads-http、temperature、top-p、top-k、spec-type、spec-draft-n-max、port、host）
- Top 进程：按 CPU Top 8、按内存 Top 8（pid/name/cpu%/mem%/rss）

### FR-08 模型与 Slot 详情

- 模型信息：名称、路径、ftype、参数量、嵌入维度、词表大小、文件体积、mmproj 体积、模态（vision/video/audio）、n_ctx、n_ctx_train、vocab_type、capabilities、owned_by
- Slot 详情：id、状态、任务 ID、prompt tokens（总/已处理/缓存）、已解码、剩余、上下文占用%、全量采样参数（temperature、dynatemp_*、top_k、top_p、min_p、top_n_sigma、xtc_*、typical_p、repeat_last_n、repeat_penalty、presence_penalty、frequency_penalty、dry_*、mirostat_*、adaptive_*、max_tokens、n_predict、n_keep、n_discard、ignore_eos、stream、n_probs、min_keep、chat_format、reasoning_format、reasoning_in_content、generation_prompt、samplers、speculative.types、timings_per_token、post_sampling_probs、backend_sampling、lora）

### FR-09 历史趋势图

- 图表类型（12 图 3 组，相关指标相邻）：
  - llama 组：Token 生成速度、预填充速度、**上下文占用（1s 采样线 + n_ctx 上限参考线）**、**MTP 接受率（每任务阶梯趋势）**
  - GPU 组：利用率（每卡）、显存（每卡）、温度（每卡）、功耗（每卡）
  - 系统组：CPU、内存（used/buff_cache 堆叠）、网络（rx/tx）、负载均值（1/5/15 三线）
- 时间窗口切换：5m / 15m / 1h
- 数据源：内存环形缓冲（llama 3600 点 @1s，host 1800 点 @2s）
- 断线时曲线断点（不插值），保留最后值

### FR-10 事件流

- 事件类型：任务开始、预填充开始（prompt 总 token 数）、任务结束（含总 token/耗时/平均速度）、模型变更、llama 上线/下线、SSH 断开/重连、阈值穿越（告警级别升级/恢复）
- 终端风格展示、自动滚动、内存保留最近 200 条
- 每条事件：时间戳 + 级别（info/warn/error）+ 描述

### FR-11 实时刷新机制

- 默认 1 秒刷新（WebSocket 推送为主通道）
- 前端刷新控制：实时（WS）/ 1s / 2s / 5s / 暂停
- 后端推送间隔按主机可配置（push_interval，默认 1s）
- 采集间隔可配置：llama 1s、SSH 2s（默认值）
- WS 断线自动降级为 HTTP 轮询并提示

### FR-12 阈值飘红

- 两级色阶：黄（warn）/ 红（danger），规则表见 §8
- 飘红效果：数字变红 + 卡片边框红光 + 脉冲动画
- 阈值可按主机覆盖
- 纯视觉，不做通知推送

### FR-13 llama-server 日志解析

- 实时 tail systemd journal（journalctl -u llama-server），解析以下日志行：
  - `print_timing ... n_decoded = X, tg = Y t/s, tg_3s = Z t/s`（生成中每 3s）：实时速度、任务平均速度、已解码数
  - `print_timing ... prompt processing, n_tokens = X, progress = P, t = T s / S t/s`（预填充中每 ~2s）：prompt 进度、prompt 速度
  - `print_timing ... prompt eval time / eval time / total time / graphs reused / draft acceptance`（任务结束汇总）：prompt/解码/总耗时、CUDA graph 复用数、**MTP 接受率/接受数/平均接受长度**
  - `release ... stop processing: n_tokens = X, truncated = T`（任务结束）：**上下文总占用、是否被截断**
  - `get_availabl ... selected slot by LCP/LRU, f_sim_best = X, f_keep = Y`（新任务）：slot 选择方式、LCP 相似度、**KV 缓存保留率**
  - `launch_slot_ ... task N, is_child = C`（任务开始）：任务 ID、是否子任务
  - 启动日志（每次启动一次）：模型路径、n_slots/n_ctx_slot/kv_unified、MTP draft context、KV 缓存类型升级（如 turbo3→q8_0）、verbosity、监听地址、警告列表（CORS、端口变更通知等）
- "llama 当前在干嘛"状态机：prompt 处理（带进度）/ 生成中（带已解码数与速度）/ 空闲
- 日志不可用（SSH 断开 / unit 名不符）→ 回退 /slots 差分，标记 log_available=false
- MTP 参数：静态配置（spec-type/spec-draft-n-max，来自命令行解析）+ 运行时统计（接受率/mean len，来自日志）同时展示


### FR-14 本地 CLI（llamalens）

- 在被监控主机**本地**运行的 htop 风格全屏 TUI，展示内容与单主机详情页（FR-03~FR-13）同源
- 零运行时依赖：单个静态二进制（Go，`CGO_ENABLED=0`），目标主机无需 Go / Python / pip
- 数据本地直采（不经后端、不写文件、不改 llama-server 状态）：
  - llama HTTP API（`/slots`@1s、`/props`+`/v1/models`@30s）
  - `/proc` 直读@2s（CPU/内存/磁盘/网络/负载/Top 进程）
  - `nvidia-smi`@2s（GPU）
  - `systemctl show` + `journalctl -u <unit> -f`（服务状态 + 日志事件流，解析规则同 FR-13）
- 交互：`q` 退出 / `p` 暂停 / `t` 趋势 / `g` GPU；终端 resize 自适应；退出自动恢复终端（无残留）
- 阈值飘红与 Web 面板同源（§8）
- 终端要求：≥80×20，需交互式 TTY；另提供 `--once` 单次文本快照（无 TTY 可用）
- 进程名 / unit / 端口须与 hosts.yaml 对应主机一致

## 5. 非功能需求


| 编号 | 需求 | 指标 |
|---|---|---|
| NFR-01 | 刷新延迟 | 数据产生到页面显示 ≤ 2s（1s 采集 + 1s 推送） |
| NFR-02 | API 响应 | /api/hosts/{id}/overview P95 < 100ms |
| NFR-03 | 降级 | 单主机 llama 离线 / SSH 断开不影响其他主机与面板自身；页面不白屏 |
| NFR-04 | 资源开销 | 面板进程 CPU < 5%（单核），内存 < 300MB（含 1h 历史） |
| NFR-05 | SSH 开销 | 每主机每 2s 一条批量命令，一次网络往返，全部只读 |
| NFR-06 | 安全 | 凭证不进 Git；面板监听 0.0.0.0:8000（局域网内），无认证（局域网内部工具，风险已声明） |
| NFR-07 | 扩展性 | 新增主机 = 加一条配置；新增指标 = 扩展采集层 + 快照字段 |
| NFR-08 | 兼容性 | Chrome/Edge 最新版；1920×1080 基准，1366×768 可用；响应式布局（内容全宽，窄窗口无横向滚动，断点 1500/1100/640px） |

## 6. 验收标准

| 编号　| 标准　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　　| 验证方式　　　　　　　　　　　　　|
| -------| -------------------------------------------------------------------------------------------------| -----------------------------------|
| AC-01 | 门户页显示 ai.lan 主机卡片，状态在线，模型 Qwen3.8-27B-Q6_K（27.32B）　　　　　　　　　　　　　 | 打开 /　　　　　　　　　　　　　　|
| AC-02 | 有任务运行时详情页 token 生成速度 > 0 且与实际速度吻合；空闲时为 0　　　　　　　　　　　　　　　| 对照主机上运行中任务 / 发送小请求 |
| AC-03 | GPU 面板数据（显存/温度/功耗）与 nvidia-smi 直连一致　　　　　　　　　　　　　　　　　　　　　　| 对照　　　　　　　　　　　　　　　|
| AC-04 | CPU/内存/磁盘数据与 free -h / df -h 一致　　　　　　　　　　　　　　　　　　　　　　　　　　　　| 对照　　　　　　　　　　　　　　　|
| AC-05 | 进程卡命令行与 ps 输出一致，参数表解析正确　　　　　　　　　　　　　　　　　　　　　　　　　　　| 对照　　　　　　　　　　　　　　　|
| AC-06 | 页面 1s 刷新，刷新控制（1s/2s/5s/暂停）生效　　　　　　　　　　　　　　　　　　　　　　　　　　 | 目测 + 浏览器网络面板　　　　　　 |
| AC-07 | 指标超阈值（如 GPU 利用率 ≥90%）时对应数字与卡片边框变红　　　　　　　　　　　　　　　　　　　　| 制造高负载或临时调低阈值　　　　　|
| AC-08 | 历史曲线连续，窗口切换（5m/15m/1h）生效　　　　　　　　　　　　　　　　　　　　　　　　　　　　 | 目测　　　　　　　　　　　　　　　|
| AC-09 | 事件流记录任务开始/结束事件（含统计）　　　　　　　　　　　　　　　　　　　　　　　　　　　　　 | 任务运行中观察　　　　　　　　　　|
| AC-10 | 停掉 llama-server → 门户卡片变红"离线"，详情页降级显示；恢复后自动恢复　　　　　　　　　　　　　| 停/起服务　　　　　　　　　　　　 |
| AC-11 | 改错 SSH 端口 → 主机区显示黄色"不可用"，llama 区正常　　　　　　　　　　　　　　　　　　　　　　| 配置变更测试　　　　　　　　　　　|
| AC-12 | hosts.yaml/.env（含密码）不在 Git 中；.gitignore 生效　　　　　　　　　　　　　　　　　　　　　 | git status　　　　　　　　　　　　|
| AC-13 | 增加第二台主机配置（可指向同一机器的测试条目）→ 门户出现 2 张卡片　　　　　　　　　　　　　　　 | 配置测试　　　　　　　　　　　　　|
| AC-14 | 日志解析：当前状态卡正确显示阶段（prompt 处理/生成中/空闲）、任务 ID、实时速度与日志 tg_3s 一致 | 对照 journalctl　　　　　　　　　 |
| AC-15 | 上下文占用显示 n_tokens/n_ctx 与剩余量，与日志 release 行一致；上下文占用趋势图连续　　　　　　 | 对照 journalctl　　　　　　　　　 |
| AC-16 | MTP 卡显示接受率/接受数/mean len，与日志 draft acceptance 行一致　　　　　　　　　　　　　　　　| 对照 journalctl　　　　　　　　　 |

## 7. 数据项清单（80+ 字段）

> 来源：H = llama-server HTTP API，S = SSH 只读采集，L = 日志解析（journalctl -u llama-server），C = 后端计算（差分/聚合）

### 7.1 模型（H: /props, /v1/models；S: ls -l）

| 字段　　　　　　　　　　| 来源 | 说明（ai.lan 实测值）　　　　　　　　　　　　　　|
| -------------------------| ------| --------------------------------------------------|
| model.name　　　　　　　| H　　| 模型名（路径）　　　　　　　　　　　　　　　　　 |
| model.path　　　　　　　| H　　| /share/AI/LLM/unsloth/Qwen3.8-27B-Q6_K.gguf　　　|
| model.ftype　　　　　　 | H　　| 量化类型（Q6_K）　　　　　　　　　　　　　　　　 |
| model.n_params　　　　　| H　　| 参数量（27,320,697,856 ≈ 27.32B）　　　　　　　　|
| model.n_embd　　　　　　| H　　| 嵌入维度（5120）　　　　　　　　　　　　　　　　 |
| model.n_vocab　　　　　 | H　　| 词表大小（248320）　　　　　　　　　　　　　　　 |
| model.n_ctx　　　　　　 | H　　| 运行上下文长度（262144）　　　　　　　　　　　　 |
| model.n_ctx_train　　　 | H　　| 训练上下文长度（262144）　　　　　　　　　　　　 |
| model.vocab_type　　　　| H　　| 词表类型（2）　　　　　　　　　　　　　　　　　　|
| model.file_size　　　　 | H/S　| 模型文件体积（22,873,411,584 B ≈ 22.87GB）　　　 |
| model.mmproj_path　　　 | S　　| 视觉投影文件路径（Qwen3.8-27B-mmproj-BF16.gguf） |
| model.mmproj_size　　　 | S　　| mmproj 文件体积（889MB）　　　　　　　　　　　　 |
| model.modalities.vision | H　　| 视觉支持（true）　　　　　　　　　　　　　　　　 |
| model.modalities.video　| H　　| 视频支持（true）　　　　　　　　　　　　　　　　 |
| model.modalities.audio　| H　　| 音频支持（false）　　　　　　　　　　　　　　　　|
| model.capabilities　　　| H　　| completion, multimodal　　　　　　　　　　　　　 |
| model.owned_by　　　　　| H　　| llamacpp　　　　　　　　　　　　　　　　　　　　 |

### 7.2 服务（S: systemctl）

| 字段　　　　　　　　| 说明（ai.lan 实测值）　　　　　　　　 |
| ---------------------| ---------------------------------------|
| service.unit　　　　| llama-server.service　　　　　　　　　|
| service.description | llama-server Qwen3.8-27B (TurboQuant) |
| service.active　　　| active (running)　　　　　　　　　　　|
| service.since　　　 | 启动时间（2026-08-28 15:53:10）　　　 |
| service.cpu_total　 | 服务级 CPU 累计（9min 57.800s）　　　 |
| service.memory　　　| 服务级内存（4.6G）　　　　　　　　　　|
| service.memory_peak | 内存峰值（4.6G）　　　　　　　　　　　|
| service.tasks　　　 | Tasks 数（10）　　　　　　　　　　　　|

### 7.3 进程（S: /proc/<pid>/stat, ps, cmdline）

| 字段 | 说明 |
|---|---|
| process.found | 是否找到进程 |
| process.pid | 31255 |
| process.cpu_pct_realtime | 实时 CPU%（差分，相对单核） |
| process.cpu_pct_lifetime | 累计 CPU%（ps） |
| process.mem_pct | 内存百分比（16.0%） |
| process.rss_mb | 常驻集（≈5153MB） |
| process.vsz_mb | 虚拟内存 |
| process.threads | 线程数 |
| process.elapsed | 运行时长 |
| process.cmdline | 完整命令行 |
| process.flags.* | 解析参数（model/mmproj/n_gpu_layers/flash_attn/tensor_split/batch/ubatch/np/ctx_size/kv_offload/cache_type_k/cache_type_v/fit/threads/threads_batch/threads_http/temperature/top_p/top_k/spec_type/spec_draft_n_max/port/host） |

### 7.4 Slot（H: /slots）

| 字段 | 说明 |
|---|---|
| slot.id | 槽位号（0） |
| slot.is_processing | 是否处理中 |
| slot.id_task | 任务 ID（5199） |
| slot.n_ctx | 槽位上下文长度（262144） |
| slot.n_prompt_tokens | prompt token 总数（19032） |
| slot.n_prompt_tokens_processed | prompt 已处理（626） |
| slot.n_prompt_tokens_cache | prompt 缓存命中（0） |
| slot.n_decoded | 已解码数（795） |
| slot.n_remain | 剩余 token（31205） |
| slot.ctx_used | 上下文占用（C: prompt+decoded） |
| slot.speculative | 是否启用投机解码（true） |
| slot.params.* | 全量采样参数（temperature/dynatemp_range/dynatemp_exponent/top_k/top_p/min_p/top_n_sigma/xtc_probability/xtc_threshold/typical_p/repeat_last_n/repeat_penalty/presence_penalty/frequency_penalty/dry_multiplier/dry_base/dry_allowed_length/dry_penalty_last_n/mirostat/mirostat_tau/mirostat_eta/adaptive_target/adaptive_decay/max_tokens/n_predict/n_keep/n_discard/ignore_eos/stream/n_probs/min_keep/chat_format/reasoning_format/reasoning_in_content/generation_prompt/samplers/speculative.types/timings_per_token/post_sampling_probs/backend_sampling/lora） |

### 7.5 速度（L: 日志为主，C: 差分兜底）

| 字段 | 来源 | 说明 |
|---|---|---|
| speed.gen_tps | L/C | 生成速度 tok/s（日志 tg_3s 为主，/slots 差分兜底） |
| speed.gen_tg_tps | L | 任务平均生成速度（日志 tg） |
| speed.prompt_tps | L/C | prompt 处理速度 tok/s（日志为主，/slots 差分兜底） |
| speed.prompt_progress | L | prompt 处理进度 0-1（日志 progress） |
| speed.prompt_total_tokens | L | prompt 总 token 数（日志 n_tokens） |
| speed.prompt_elapsed_s | L | prompt 已耗时秒（日志 t） |
| log.last_prefill | L | 最近一次预填充 {speed, ts, progress, n_tokens}（持久，每条 prompt 行更新，重启清空） |
| speed.task_total_tokens | L | 当前任务累计 token（事件） |
| speed.task_duration_s | L | 当前任务耗时（事件，日志 total time） |
| speed.task_avg_tps | L | 当前任务平均速度（事件，日志 eval time） |

### 7.6 GPU（S: nvidia-smi，每卡）

| 字段　　　　　　　| 说明（ai.lan 实测值）　　　　　 |
| -------------------| ---------------------------------|
| gpu.index　　　　 | 卡号（0/1）　　　　　　　　　　 |
| gpu.name　　　　　| 型号（NVIDIA GeForce RTX 3080） |
| gpu.driver　　　　| 驱动版本（580.159.03）　　　　　|
| gpu.cuda　　　　　| CUDA 版本（13.0）　　　　　　　 |
| gpu.util_pct　　　| GPU 利用率 %　　　　　　　　　　|
| gpu.mem_util_pct　| 显存控制器利用率 %　　　　　　　|
| gpu.mem_total_mb　| 显存总量（20480）　　　　　　　 |
| gpu.mem_used_mb　 | 显存已用（18459/17639）　　　　 |
| gpu.mem_free_mb　 | 显存空闲　　　　　　　　　　　　|
| gpu.temp_c　　　　| 温度（54/49°C）　　　　　　　　 |
| gpu.power_w　　　 | 当前功耗（141.5/137.3W）　　　　|
| gpu.power_limit_w | 功耗上限（280W）　　　　　　　　|
| gpu.fan_pct　　　 | 风扇转速　　　　　　　　　　　　|
| gpu.clock_mhz　　 | 图形频率　　　　　　　　　　　　|
| gpu.mem_clock_mhz | 显存频率　　　　　　　　　　　　|
| gpu.pcie_gen　　　| PCIe 代（3）　　　　　　　　　　|
| gpu.pcie_width　　| PCIe 宽度（16/4）　　　　　　　 |
| gpu.pstate　　　　| 性能状态（P2）　　　　　　　　　|
| gpu.temp_mem_c　　| 显存温度 °C（HBM 卡有值，消费卡 [N/A]→null） |
| gpu.ecc_corrected | ECC 可纠正错误累计（volatile，[N/A]→null） |
| gpu.ecc_uncorrected | ECC 不可纠正错误累计（volatile，>0 前端飘红） |
| gpu.throttle　　　| 降频原因位掩码（0=正常；Not Active/十进制/0x 十六进制均解析） |
| gpu.cuda　　　　　| CUDA 版本（13.0） |
| gpu.apps[]　　　　| 占用进程（pid/name/显存）　　　 |

### 7.7 CPU（S: /proc/stat, /proc/cpuinfo）

| 字段 | 说明 |
|---|---|
| cpu.model | Intel Core i5-8500 |
| cpu.cores | 核数（6，无超线程） |
| cpu.usage_pct | 整机利用率（C 差分） |
| cpu.per_core_pct[] | 每核利用率（C 差分，6 个值） |
| cpu.load1/load5/load15 | 负载（1.09/0.75/0.58） |
| cpu.mhz | 主频（3000） |

### 7.8 内存（S: /proc/meminfo）

| 字段 | 说明 |
|---|---|
| mem.total_mb | 总量（31Gi） |
| mem.used_mb | 已用（5.8Gi） |
| mem.free_mb | 空闲 |
| mem.buff_cache_mb | buff/cache（25Gi） |
| mem.available_mb | 可用 |
| mem.swap_total_mb | swap 总量（8Gi） |
| mem.swap_used_mb | swap 已用 |

### 7.9 磁盘（S: df, /proc/diskstats）

| 字段 | 说明 |
|---|---|
| disk.mounts[] | 挂载点列表（/） |
| disk.size_gb/used_gb/avail_gb/use_pct | 容量（1.2T/801G/347G/70%） |
| disk.read_mb_s/write_mb_s | 读写速率（C 差分） |

### 7.10 网络（S: /proc/net/dev）

| 字段 | 说明 |
|---|---|
| net.ifaces[] | 网卡列表（排除 lo） |
| net.rx_mb_s/tx_mb_s | 速率（C 差分） |
| net.rx_total/tx_total | 累计 |

### 7.11 系统（S）

| 字段 | 说明 |
|---|---|
| sys.hostname | ai |
| sys.kernel | 6.8.0-124-generic |
| sys.os | OS 版本（/etc/os-release） |
| sys.uptime_s | 运行时长 |
| sys.procs | 进程总数 |

### 7.12 事件（C）

| 字段 | 说明 |
|---|---|
| event.ts | 时间戳 |
| event.level | info/warn/error |
| event.type | task_start/prefill_start/task_end/model_change/llama_up/llama_down/ssh_down/ssh_up/llama_boot/alert |
| event.msg | 描述（task_end 含总 token/耗时/平均速度/MTP 接受率/上下文占用；prefill_start 含 prompt 总 token 数；alert 含指标/值/阈值/级别） |

### 7.13 日志（L: journalctl -u llama-server）

| 字段　　　　　　　　　　　　　　　　　　　　　　　| 说明（ai.lan 实测值）　　　　　　　　　　　　　　　　　 |
| ---------------------------------------------------| ---------------------------------------------------------|
| log.available　　　　　　　　　　　　　　　　　　 | 日志通道是否可用　　　　　　　　　　　　　　　　　　　　|
| log.state.phase　　　　　　　　　　　　　　　　　 | 当前阶段：prompt_processing / decoding / idle　　　　　 |
| log.state.task_id　　　　　　　　　　　　　　　　 | 当前任务 ID（14847）　　　　　　　　　　　　　　　　　　|
| log.state.n_decoded　　　　　　　　　　　　　　　 | 当前任务已解码数（2843）　　　　　　　　　　　　　　　　|
| log.state.tg_3s_tps　　　　　　　　　　　　　　　 | 3 秒窗口实时速度（28.46）　　　　　　　　　　　　　　　 |
| log.state.tg_tps　　　　　　　　　　　　　　　　　| 任务平均速度（26.21）　　　　　　　　　　　　　　　　　 |
| log.state.prompt_progress　　　　　　　　　　　　 | prompt 进度 0-1（0.92）　　　　　　　　　　　　　　　　 |
| log.state.prompt_speed_tps　　　　　　　　　　　　| prompt 速度（516.98）　　　　　　　　　　　　　　　　　 |
| log.state.is_child　　　　　　　　　　　　　　　　| 是否子任务（0）　　　　　　　　　　　　　　　　　　　　 |
| log.context.used　　　　　　　　　　　　　　　　　| 上下文已用（n_tokens，201047）　　　　　　　　　　　　　|
| log.context.total　　　　　　　　　　　　　　　　 | 上下文总量（262144）　　　　　　　　　　　　　　　　　　|
| log.context.pct　　　　　　　　　　　　　　　　　 | 百分比（76.7）　　　　　　　　　　　　　　　　　　　　　|
| log.context.remaining　　　　　　　　　　　　　　 | 剩余（61097）　　　　　　　　　　　　　　　　　　　　　 |
| log.context.truncated　　　　　　　　　　　　　　 | 是否被截断（0）　　　　　　　　　　　　　　　　　　　　 |
| log.mtp.acceptance　　　　　　　　　　　　　　　　| MTP 接受率（0.698）　　　　　　　　　　　　　　　　　　 |
| log.mtp.accepted　　　　　　　　　　　　　　　　　| 接受数（1925）　　　　　　　　　　　　　　　　　　　　　|
| log.mtp.generated　　　　　　　　　　　　　　　　 | 生成数（2757）　　　　　　　　　　　　　　　　　　　　　|
| log.mtp.mean_len　　　　　　　　　　　　　　　　　| 平均接受长度（3.09）　　　　　　　　　　　　　　　　　　|
| log.kv.f_keep　　　　　　　　　　　　　　　　　　 | KV 缓存保留率（1.000）　　　　　　　　　　　　　　　　　|
| log.kv.f_sim_best　　　　　　　　　　　　　　　　 | LCP 相似度（0.988）　　　　　　　　　　　　　　　　　　 |
| log.kv.selection　　　　　　　　　　　　　　　　　| slot 选择方式（LCP/LRU）　　　　　　　　　　　　　　　　|
| log.graphs_reused　　　　　　　　　　　　　　　　 | CUDA graph 复用数（15340）　　　　　　　　　　　　　　　|
| log.task.prompt_ms/prompt_tokens/prompt_speed_tps | 任务汇总：prompt 耗时/token/速度　　　　　　　　　　　　|
| log.task.eval_ms/decoded_tokens/gen_speed_tps　　 | 任务汇总：解码耗时/token/速度　　　　　　　　　　　　　 |
| log.task.total_ms/total_tokens　　　　　　　　　　| 任务汇总：总耗时/总 token　　　　　　　　　　　　　　　 |
| log.boot.pid/started_at　　　　　　　　　　　　　 | 启动进程与时间（55646 / 17:20:41）　　　　　　　　　　　|
| log.boot.verbosity　　　　　　　　　　　　　　　　| 日志级别（3）　　　　　　　　　　　　　　　　　　　　　 |
| log.boot.n_slots/n_ctx_slot/kv_unified　　　　　　| 槽位数/上下文/kv_unified（1/262144/false）　　　　　　　|
| log.boot.mtp_draft　　　　　　　　　　　　　　　　| 是否创建 MTP draft context（true）　　　　　　　　　　　|
| log.boot.kv_cache_upgrade　　　　　　　　　　　　 | KV 缓存类型升级（turbo3→q8_0，auto-asymmetric GQA 6:1） |
| log.boot.listening　　　　　　　　　　　　　　　　| 监听地址（http://0.0.0.0:8080）　　　　　　　　　　　　 |
| log.boot.warnings[]　　　　　　　　　　　　　　　 | 警告列表（CORS 全开、端口变更通知等）　　　　　　　　　 |

## 8. 阈值规则（默认值，可按主机覆盖）

| 指标 | 黄（warn） | 红（danger） |
|---|---|---|
| GPU 利用率 | ≥80% | ≥90% |
| GPU 显存 | ≥85% | ≥95% |
| GPU 温度 | ≥75°C | ≥85°C |
| GPU 功耗 | ≥85% 上限 | ≥95% 上限 |
| CPU 利用率 | ≥80% | ≥90% |
| 内存使用 | ≥85% | ≥95% |
| 磁盘使用 | ≥80% | ≥90% |
| 上下文占用 | ≥80% | ≥90% |
| MTP 接受率 | <80% | <65% |
| llama 离线 | — | 红 |
| SSH 断开 | — | 黄 |

## 9. 范围外

- 告警推送（邮件/IM/webhook）—— 后续迭代
- 历史数据持久化（SQLite/InfluxDB）—— 后续迭代
- 多用户认证与权限 —— 局域网内部工具，暂不做
- llama-server 远程控制（启停/重启/改参）—— 只读监控
- Windows 客户端 / 移动端适配 —— 仅桌面浏览器
- 非 llama 服务监控（docker 等详细监控）

## 10. 后续迭代（不在本期）

1. 告警推送：webhook / Server酱 / Bark
2. 历史持久化 + 长期趋势（7d/30d）
3. llama-server --metrics 接入（若实例以该参数重启，切换为更精确数据源）
4. SSH key 认证（替代密码）
5. Docker/K8s 打包
6. 同主机多模型/多实例（多个 llama-server 进程）
7. Token 成本估算（速度 × 单价）

## 11. 附录 A：实测环境（ai.lan，2026-08-28）

- 主机名 ai，内核 6.8.0-124-generic（Ubuntu），IPv6 2408:8266:401:2eca::fa6
- CPU：Intel Core i5-8500，6 核（无超线程），3.00GHz
- 内存：31Gi（buff/cache 25Gi），swap 8Gi
- GPU：2× NVIDIA GeForce RTX 3080（各 20GB），驱动 580.159.03
  - GPU0：PCIe gen3 x16；GPU1：PCIe gen3 x4
- 磁盘：/dev/nvme0n1p3 1.2T，已用 70%（801G/347G）
- llama-server：systemd unit llama-server.service，二进制 /home/wx/llama-cpp-turboquant-new/build/bin/llama-server
  - 完整命令行：
    ```
    -m /share/AI/LLM/unsloth/Qwen3.8-27B-Q6_K.gguf
    --mmproj /share/AI/LLM/unsloth/Qwen3.8-27B-mmproj-BF16.gguf
    --n-gpu-layers all --flash-attn enabled -ts 24,20 -b 1024 -ub 512 -np 1
    --ctx-size 262144 --kv-offload --cache-type-k turbo3 --cache-type-v turbo3
    --fit off --threads 6 --threads-batch 6 --threads-http 1
    --temperature 0.3 --top-p 0.9 --top-k 40
    --spec-type draft-mtp --spec-draft-n-max 3
    --port 8080 --host 0.0.0.0
    ```
- 可用 API 端点：/health、/slots、/props、/v1/models（实测）；/metrics 未启用（501）
- 模型：Qwen3.8-27B-Q6_K.gguf（22.87GB，27.32B 参数，n_embd 5120，n_vocab 248320）+ mmproj（889MB，vision+video）
- 日志（journalctl -u llama-server，实测关键行）：
  - 生成中（每 3s）：`slot print_timing: id 0 | task 14847 | n_decoded = 2843, tg = 26.21 t/s, tg_3s = 28.46 t/s`
  - 预填充（每 ~2s）：`slot print_timing: id 0 | task 8050 | prompt processing, n_tokens = 2048, progress = 0.92, t = 3.96 s / 516.98 tokens per second`
  - 任务结束汇总：`prompt eval time = 55930.81 ms / 23069 tokens (412.46 t/s)` + `eval time = 225389.07 ms / 5930 tokens (26.31 t/s)` + `total time = 281319.88 ms / 28999 tokens` + `graphs reused = 14433` + `draft acceptance = 0.68784 (3995 accepted / 5808 generated), mean len = 3.06`
  - 任务结束：`slot release: id 0 | task 12885 | stop processing: n_tokens = 193477, truncated = 0`
  - 新任务：`slot get_availabl: id 0 | task -1 | selected slot by LCP similarity, f_sim_best = 0.988 (> 0.100 thold), f_keep = 1.000`
  - 任务开始：`slot launch_slot_: id 0 | task 11793 | processing task, is_child = 0`
  - 启动：`loading model '...'`、`n_slots = 1, n_ctx_slot = 262144, kv_unified = 'false'`、`creating MTP draft context`、`auto-asymmetric: GQA ratio 6:1 — upgrading K from turbo3 to q8_0`、`listening on http://0.0.0.0:8080`
- 日志关键观察（2026-08-28）：
  - 上下文跨任务持续增长：130453 → 135504 → 146806 → 154804 → 159681 → 161641 → 164477 → 193477 → 201047（已达 262144 的 76.7%，长对话累积，存在截断风险）
  - MTP 接受率随上下文增长下降：0.931 → 0.850 → 0.702 → 0.688 → 0.698（mean len 3.79 → 3.06）
  - prompt 速度：冷启动 ~1100 t/s（无缓存），热 ~500 t/s（KV 缓存命中）
